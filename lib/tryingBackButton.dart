import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class tryingBachButton extends StatelessWidget {
  static const routeName ='tryingBackButton';

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.brown,
    );
  }
}
